import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tap-auth-demo-firebase-with-magiclink',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
